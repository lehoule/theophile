# AGENTS.md

- This is an Astro static site with a Cloudflare Worker/D1 comment API.
- Keep changes focused and preserve imported article content.
- Every new functionality must include tests.
- Pin every dependency to an exact version; never use `^` or `~`.
- Before handoff, run `npm run format`, `npm run lint`, `npm test`, and `npm run build`.
